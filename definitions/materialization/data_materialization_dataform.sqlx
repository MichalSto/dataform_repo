config { type: "table",
        description: "table materialized by DataForm"}

select OpenTime, OpenPrice, ClosePrice, datecurrent, ARRAY
  (SELECT 1 UNION ALL
   SELECT 2 UNION ALL
   SELECT 4) AS new_array from ${ref("data_selection_dataform")}